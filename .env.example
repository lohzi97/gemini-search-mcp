# =============================================================================
# Gemini Search MCP - Environment Variables Configuration
# =============================================================================
# Copy this file to .env and configure values as needed
# Note: Most variables have sensible defaults and are optional

# -----------------------------------------------------------------------------
# Gemini Configuration
# -----------------------------------------------------------------------------

# Gemini model to use for research (passed via --model flag)
# Any valid model name is allowed (e.g., gemini-2.5-flash, gemini-2.5-pro, gemini-2.0-flash-exp)
# If not set, the Gemini CLI will auto-select the appropriate model
# GEMINI_MODEL=gemini-2.5-flash

# Secondary Gemini model for lightweight tasks (JSON correction, webpage fetching)
# Uses a faster/smaller model for tasks that don't require deep reasoning
# Used for: JSON correction in search/deep_search, and HTML fetching in fetch_webpage
# If not set, uses the same behavior as GEMINI_MODEL (auto-select if not set)
# SECONDARY_GEMINI_MODEL=gemini-2.5-flash

# Maximum time in milliseconds to wait for Gemini CLI to complete
# Default: 300000 (5 minutes)
# GEMINI_SEARCH_TIMEOUT=300000

# Custom system prompt template
# Use {topic}, {depth}, and {model} as placeholders
# Default: built-in prompt (see prompts/research-prompt.md)
# GEMINI_SYSTEM_PROMPT=

# Maximum number of retries for JSON parsing when CLI output is malformed
# Default: 3
# JSON_MAX_RETRIES=3

# Maximum research iterations (search/scrape cycles) for Gemini CLI
# Default: 10
# GEMINI_CLI_MAX_ITERATIONS=10

# Maximum number of verification rounds for deep_search tool
# The deep_search tool performs server-orchestrated iterative research with verification
# Each round verifies and enhances the previous result
# Default: 5
# DEEP_SEARCH_MAX_ITERATIONS=5

# -----------------------------------------------------------------------------
# Firecrawl Configuration (Optional)
# -----------------------------------------------------------------------------

# Firecrawl API key for cloud API
# Get a free key at https://www.firecrawl.dev
# If not set, Firecrawl MCP will fail to connect and Gemini CLI will fall back to web_fetch
# FIRECRAWL_API_KEY=fc-your-api-key-here

# Firecrawl API URL for self-hosted instances
# Example: https://firecrawl.your-domain.com
# If set, FIRECRAWL_API_KEY may also be required depending on your instance config
# FIRECRAWL_API_URL=

# -----------------------------------------------------------------------------
# Configuration Directory
# -----------------------------------------------------------------------------

# Directory for project-level Gemini CLI settings
# Default: Platform-appropriate (~/.config/gemini-search-mcp/ on Linux)
# GEMINI_SEARCH_CONFIG_DIR=/path/to/config

# -----------------------------------------------------------------------------
# MCP Server Configuration (HTTP mode only)
# -----------------------------------------------------------------------------

# Port for the MCP HTTP server to listen on
# Used by gemini-search-mcp-http binary
# Default: 3000
# MCP_SERVER_PORT=3000

# Host binding for the MCP HTTP server
# Default: 127.0.0.1 (localhost only - set to 0.0.0.0 for external access)
# MCP_SERVER_HOST=127.0.0.1

# -----------------------------------------------------------------------------
# Debug and Logging
# -----------------------------------------------------------------------------

# Enable verbose logging for troubleshooting
# Default: false
# DEBUG=false

# Interval in milliseconds for logging progress messages to console
# Default: 30000 (30 seconds)
# PROGRESS_LOG_INTERVAL=30000
